import React, { useState } from "react";
import { X, Star } from "lucide-react";
export default function LogVisitDialog({ restaurant, onClose, onSubmit }) { const [rating,setRating]=useState(5); const [review,setReview]=useState(""); if(!restaurant)return null;
return <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#071b2d]/60"><form onSubmit={e=>{e.preventDefault();onSubmit({rating,review})}} className="w-full max-w-md rounded-t-[2rem] bg-white p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-[#EB8317]">Log a visit</p><h2 className="text-xl font-black">{restaurant.name}</h2></div><button type="button" onClick={onClose} className="rounded-full bg-slate-100 p-2"><X/></button></div><p className="mt-6 text-sm font-bold">How was it?</p><div className="mt-2 flex gap-2">{[1,2,3,4,5].map(n=><button type="button" key={n} onClick={()=>setRating(n)} aria-label={`${n} stars`}><Star className={`h-8 w-8 ${n<=rating?"fill-[#F3C623] text-[#F3C623]":"text-slate-200"}`}/></button>)}</div><textarea value={review} onChange={e=>setReview(e.target.value)} placeholder="What should your friends know?" className="mt-5 h-28 w-full resize-none rounded-2xl border p-4 text-sm outline-none focus:ring-2 focus:ring-[#F3C623]"/><button className="mt-4 w-full rounded-2xl bg-[#F3C623] py-3.5 font-extrabold">Add to my log</button></form></div>; }